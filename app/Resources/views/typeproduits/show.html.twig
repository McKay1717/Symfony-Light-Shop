{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typeproduit</h1>

    <table>
        <tbody>
            <tr>
                <th>Libelle</th>
                <td>{{ typeproduit.libelle }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ typeproduit.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typeproduits_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typeproduits_edit', { 'id': typeproduit.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
